pakit:
  command:
    timeout: 120
  defaults:
    repo: stable
  log:
    enabled: true
    file: /tmp/test_pakit/main.log
  paths:
    link: /tmp/test_pakit/link
    prefix: /tmp/test_pakit/builds
    recipes: /tmp
    source: /tmp/test_pakit/src
ag:
  repo: unstable
